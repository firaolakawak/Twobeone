import { render, screen } from '@testing-library/react'; import { afterEach, describe, expect, it, vi } from 'vitest'; import { AuditLog } from '../AuditLog';
describe('AuditLog security console',()=>{afterEach(()=>vi.restoreAllMocks());it('renders persisted events and inspection details',async()=>{vi.spyOn(globalThis,'fetch').mockResolvedValue({ok:true,json:async()=>({total:1,entries:[{id:'audit-1',event:'admin.user_deleted',category:'admin',userId:'admin-1',userName:'Morgan Admin',userEmail:'morgan@example.com',metadata:{targetUserId:'user-2'},timestamp:'2026-08-12T10:00:00Z'}]})} as Response);render(<AuditLog accessToken="admin-token"/>);expect(await screen.findAllByText('Admin · User deleted')).toHaveLength(2);expect(screen.getByRole('button',{name:'Inspect admin.user_deleted by Morgan Admin'})).toBeInTheDocument();expect(screen.getByText(/targetUserId/)).toBeInTheDocument();expect(screen.queryByRole('button',{name:/test/i})).not.toBeInTheDocument();});});
