import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { AccountRecovery } from '../AccountRecovery';
describe('AccountRecovery console',()=>{it('renders the guided discovery workflow and safety language',()=>{render(<AccountRecovery accessToken="admin-token"/>);expect(screen.getByRole('heading',{name:'Account Recovery'})).toBeInTheDocument();expect(screen.getByRole('button',{name:/Couple history/})).toBeInTheDocument();expect(screen.getByText('Locate old data')).toBeInTheDocument();expect(screen.getByText(/Search read-only evidence/)).toBeInTheDocument();});});
